# Rollback
Restore previous /root/cobo_project/bin/api from prior image/binary; docker compose up -d --force-recreate --no-deps api
FE: prior web/dist + fix-dev-web-perms
Flags unchanged; no DB rollback needed (no data mutation)
