# BE tasks call chain
GET .../instances/{instance_id}/tasks → listInstanceTasks → authorize workflow.read → ListTasksByInstance
Department subquery without outer COALESCE returned SQL NULL when no department_memberships → Scan panic/error → 500
