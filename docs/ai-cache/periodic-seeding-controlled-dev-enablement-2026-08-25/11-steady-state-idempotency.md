# 11 — Steady state

Observed ~20s / multiple 5s ticks after first materialize:

```text
POST_FIRST_TICK cycles=57 records=69 wf=58 (unchanged)
POST_FIRST_TICK_CYCLE_GROWTH=0
POST_FIRST_TICK_RECORD_GROWTH=0
POST_FIRST_TICK_WORKFLOW_GROWTH=0
STEADY_STATE_IDEMPOTENCY=PASS
materialize log only once; seeded=52 Info every tick (known NOOP count)
```
