# Worktree baseline

Branch (both): `recovery/lost-changes-audit-20260717-153324`

Preexisting (preserved, not phase source):
- audit pack docs
- deploy-artifacts binaries/dist from deploy
- unrelated workflow authz work if present on other files

Phase source deltas only under disclosure/app + cms templates + types.
