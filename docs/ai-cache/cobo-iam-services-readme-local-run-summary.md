# README & local run — summary

- Root `README.md`: kien truc, module map, MySQL local, migration thu cong (0001 roi 0003), chay API/worker, env, han che bootstrap.
- Outbox smoke tests: `internal/platform/outbox/processor_test.go` (package `outbox_test`).

Bước kế goi y tu roadmap: integration test HTTP + MySQL, transactional outbox, hoặc day P1 business modules len MySQL.
