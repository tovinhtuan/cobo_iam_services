# 13 — Rollback

```text
RUNTIME_ROLLBACK=
  set PERIODIC_SEEDING_ENABLED=false (worker env) + restart worker
  optionally leave WORKFLOW_SNAPSHOT as required by other features

ROLLBACK_STOPS_FUTURE_RUNS=true
ROLLBACK_REMOVES_CREATED_CYCLES=false
ROLLBACK_REMOVES_CREATED_RECORDS=false
CONFIG_ROLLBACK != DATA_ROLLBACK

DATA_SIDE_EFFECTS_ALREADY_CREATED=PERSIST
DATA_CLEANUP_AFTER_FAILED_ENABLEMENT=REQUIRES_SEPARATE_REVIEW
DATA_CLEANUP_REQUIRED_IF_ABORT=SEPARATE_REVIEW

ROLLBACK_VERIFICATION=
  printenv PERIODIC_SEEDING_ENABLED=false
  counts stop increasing across ticks
```
