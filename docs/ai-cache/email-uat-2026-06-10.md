# Email Program — Final UAT (2026-06-10)

- **task type:** UAT (review-only, no code/deploy/file change)
- **objective:** User Acceptance Test of 5 P0 email journeys on DEV (88.216.208.0), account `tvttthptlvh@gmail.com`.

## Test setup discovered
- QA account = real portal user: login_id `admin.dn@example.com` / password `secret` (user `u_admin_dn`, email `tvttthptlvh@gmail.com`), Enterprise Admin of **Company X (`c_001`)** + others.
- The 5 QA emails (sent earlier) were enqueued to the recipient with **synthetic IDs** (`prop-qa-*`, `rec-qa-*`) → their exact CTAs resolve at route level but show not-found at data level. UAT journeys were therefore re-run against **real records** of the same routes/screens.
- Browser UAT via cursor-ide-browser MCP against `http://88.216.208.0:3000`.

## Journeys verified live (browser)
- **Login**: deep-link (unauth) → `/login` (deep-link preserved, no loop/404) → sign in `admin.dn@example.com`/`secret` → success.
- **Company Select**: auto-selected **Company X** on deep-link login; header shows "Công ty đang chọn Company X".
- **adhoc.proposal_rejected** → `/app/ad-hoc-proposals/019eacba-...` → proposal detail "P0 Email Test Rejection - Strategy Change", badge **Từ chối**, "Lý do từ chối: Content does not meet compliance requirements", approval history. Action = view detail / back to list. ✓
- **adhoc.proposal_approved** → `/app/disclosures/019e6515-cb0b-...` → disclosure record "Nghị quyết Hội đồng quản trị wwf" (**Published**), workflow instance (Status Đang xử lý, Step 1), Tasks with **Review / Reject** actions. ✓
- **adhoc.controller_review_requested** → `/app/ad-hoc-proposals/019e59ce-...` (pending) → proposal detail, badge **Chờ Kiểm soát duyệt**, approval history (Focal duyệt m_102), action "Rút lại" (approve is role-gated to controller m_107). ✓
- **auth.user_invitation.existing_user** + **reminder.disclosure_deadline** → CTA = portal root `http://88.216.208.0:3000` → public landing ("Vào nền tảng CoBo") → authenticated → `/app/dashboard` (Company X, 42 CBTT stats). No loop/404. ✓

## Content correctness (deployed embed binary render)
- All 5 subjects/bodies Vietnamese, no `<no value>`/UUID/enum/technical code; adhoc CTAs absolute deep-links; invitation+reminder CTAs = portal root.

## Limitations (require inbox owner)
- **Email received / visual subject+HTML in Gmail client**: cannot access `tvttthptlvh@gmail.com` inbox. Server-side delivery proven: `email_notifications.status=sent`, `email_delivery_attempts` provider=smtp status=sent attempt_no=1, `cobo_email_failed_permanent_total` unchanged.
- **Clicking the literally-sent emails' CTAs**: those carry synthetic IDs → would show not-found; journeys validated on real records instead.
- **Completing "approve" in controller-review**: role-gated to the controller account (m_107), not the QA admin.

## UX notes (non-blocking)
- reminder.disclosure_deadline CTA points at portal **root** (landing) rather than deep-linking the specific disclosure/deadline screen — works but less targeted than adhoc CTAs. Candidate improvement.
- One real proposal record has a mojibake title (`?â`) in DB test data — display artifact of bad seed data, not an email/template defect.

## Verdict
ACCEPTED at system + journey level (delivery, content, CTA→login→company-select→target-screen→action all PASS). One manual gate remains: inbox owner confirms the 5 emails arrived and render in the Gmail client.
